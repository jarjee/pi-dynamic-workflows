# Sample Project

A minimal project for workflow subagents to read and analyze during evals.

## Structure

- `src/auth.ts` — Authentication module (mock)
- `src/billing.ts` — Billing module (mock)
- `src/utils.ts` — Shared utilities (mock)

## Usage

Workflow evals use these files as read-only targets for subagent scans, reviews, and analysis.