Use the workflow tool to run a comprehensive but bounded integration test of workflow features. Include: registerPhase with multiple phases, parallel fan-out across the existing files sample-project/src/auth.ts, sample-project/src/billing.ts, and sample-project/src/utils.ts, structured output with a schema for one analysis agent, handoff between agents within phases, a small mailbox team with exactly two spawned agents (architect and reviewer), and a final synthesis phase that reports everything worked. Keep the mailbox flow supervisor-seeded and simple: after spawning and connecting the agents, use mailbox.send(...) from the workflow supervisor to send the same review contract to BOTH mailbox agents. Each mailbox agent should read its supervisor message and finish with a short report. Do not require peer acknowledgements or peer waiting in this full integration eval. No indefinite waiting, no multi-step review loops, and await all spawned handles before returning.