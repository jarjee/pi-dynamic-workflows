Use the workflow tool to run a comprehensive but bounded integration test of workflow features. Include: registerPhase with multiple phases, parallel fan-out across the existing files sample-project/src/auth.ts, sample-project/src/billing.ts, and sample-project/src/utils.ts, structured output with a schema for one analysis agent, handoff between agents within phases, a small mailbox team with exactly two spawned agents (architect and reviewer) that exchange one message each and then finish, and a final synthesis phase that reports everything worked. Keep the mailbox flow simple: no indefinite waiting, no multi-step review loops, and await all spawned handles before returning.