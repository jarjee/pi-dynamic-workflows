Use the workflow tool to test file creation with safety. Create two agents that each write to their own non-overlapping directory (alpha/ and beta/). Each agent should create a simple output file in its directory. Use a gate to validate that both files were created correctly and contain the expected content. The agents must respect file ownership boundaries — alpha agent only writes to alpha/, beta agent only to beta/. The gate should verify this.