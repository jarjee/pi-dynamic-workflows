Use the workflow tool to process the sample project files through a pipeline. First stage: read each file and count its lines. Second stage: for each file, categorize it by size (small < 50 lines, medium 50-100, large > 100). Use pipeline() so each file goes through both stages in sequence, but files are processed concurrently. End with a summary phase that reports the distribution.