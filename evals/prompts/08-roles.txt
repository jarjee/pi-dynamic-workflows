Use the workflow tool to review the sample project's architecture using different review perspectives. Create three phases: one with a reviewer role that assesses code quality, one with a critic role that challenges the review's assumptions, and one with a synthesizer role that combines both into a final assessment. Use the package:reviewer, package:critic, and package:synthesizer roles on the agents.