Use the workflow tool to classify the sample project's auth module. Set up a phase that reads src/auth.ts and classifies it using structured output with these fields: moduleName (string), language (string), functionCount (number), hasErrorHandling (boolean). Use a schema on the agent so it must produce structured data. Add a second phase that validates the classification matches the actual file. If the validation fails, the phase should retry.