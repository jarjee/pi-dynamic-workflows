Use the workflow tool to test a self-correcting agent. Create a phase where an agent must produce a greeting that contains BOTH the words 'hello' AND 'world'. Put a gate on the phase that checks for both words. If the gate fails, the phase should retry up to 3 times with the failure context injected into the agent's prompt. After the gate passes, add a second phase that confirms the greeting is valid.